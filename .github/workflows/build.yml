name: Docker Compose Up
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: cat "POSTGRES_USER=admin\nPOSTGRES_PASSWORD=admin" > .env && docker-compose up -d