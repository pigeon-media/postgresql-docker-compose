name: Docker Compose Up
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: cp .env.example .env && docker-compose --env-file .env up -d