version: '3'
services: 
  db:
    image: postgres
    restart: unless-stopped
    ports:
      - '127.0.0.1:5430:5432'
    env_file: 
      - .env
    volumes:
      - db_data:/var/lib/postgresql/data/

volumes:
  db_data: